pipeline {
    agent any
    stages {
        stage('Add') {
            steps {
                 sh "chmod 777 add2.sh"
                 sh "./add2.sh"
                
            } 
        }
    }
}

